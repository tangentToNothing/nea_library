<%= form_for(@targetgroup) do |f| %>
    <%= hidden_field_tag :locale, I18n.locale %>
  <% if @targetgroup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@targetgroup.errors.count, "error") %> prohibited this targetgroup from being saved:</h2>

      <ul>
      <% @targetgroup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
